name: ASCII_Script

on: [push]

jobs: 
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v6
          
        - name: List Repo Files
          run: ls -ltra

        - name: Executing Shell Script
          run: |
            chmod +x srihari-kodati/actions-1/.github/workflows/ ascii-script.sh
            ./srihari-kodati/actions-1/.github/workflows/ ascii-script.sh
